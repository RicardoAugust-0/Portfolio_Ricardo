export interface ProjectCardProps {
  title: string;
  description: string;
  tags: string[];
}

export interface FeaturedProjectProps {
  title: string;
  description: string;
  longDescription: string;
  tags: string[];
}
